
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/platform_contacts
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver

logging:
  level:
    org:
      hibernate:
        SQL: debug
        type:
          descriptor:
            sql:
              BasicBinder: trace